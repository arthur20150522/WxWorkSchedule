# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Production
# dist  — un-ignored for server/dist/ (tracked to ensure remote always has compiled JS)
build

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# Application Data (Sensitive)
db.json
config.json
server/db.json
server/config.json
server/.user
server/users/
server/.env
server/data/qr/
.user
users/
# runtime send audit (recipient names / timing) — never publish
wx4py_send_audit.jsonl

# Build artifacts
# server/dist/  — TRACKED: ensures remote always has latest compiled JS
client/dist/
__pycache__/

# Assistant tool memory
.workbuddy/

# Screenshots of the remote desktop (may contain private chat content)
tools/screenshot_remote.png

# Temp / debug files
env_check.txt
deploy_log.txt
ssh_test.txt
bridge_check.txt
.trae